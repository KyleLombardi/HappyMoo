// src/styles/navigationStyles.js
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    tabBarStyle: {
        backgroundColor: '#F0F0F0', // Apple-like gray background
        borderTopColor: 'transparent', // Hides the top border
    },
    labelStyle: {
        fontSize: 12,
        fontWeight: 'bold',
    }
});
